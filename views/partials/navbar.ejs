<div class="topbar">
  <div class="brand">🔐 <%= dbName %></div>
  <div style="display:flex; align-items:center; gap:10px;">
    <span class="badge badge-<%= session.role %>"><%= session.role %></span>
    <span style="font-size:13px; font-weight:600;"><%= session.username %></span>
  </div>
</div>
